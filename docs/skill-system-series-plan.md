# 技能系统深度系列规划

## 系列定位

这组文章不写成 `做一个火球术教程`，也不写成 `Unity 技能框架 API 速查`。

它真正要解决的问题是：

`把技能系统拆成一条稳定的执行链，讲清楚输入意图怎样变成施法请求，怎样经过校验、目标选择、命中、效果执行、Buff 规则，再延伸到表现、AI、多人同步、工具链和测试。`

一句话说，这个系列的重点不是“某个技能怎么写”，而是：

`技能系统作为一条完整执行链，到底该怎样分层、怎样定边界、怎样长期维护。`

## 为什么值得单独做成系列

如果只把技能系统当成零散文章去写，后面很快会遇到三个问题：

- `Skill / Effect / Buff / Attribute / Tag / Cooldown / Resource` 会不断混写，越写越像一个大对象或一组大 if
- 输入、表现、AI、网络会被默认吞进“技能逻辑”，最后失去系统边界
- 工程后期真正会爆炸的问题，往往不在“能不能放出一个技能”，而在于同步、工具、回归和跨框架映射

所以更稳的做法不是继续补零散专题，而是先固定一条上位主线：

`问题空间 -> 概念边界 -> 数据骨架 -> 生命周期 -> 执行主链 -> 世界变化 -> 跨系统边界 -> 工程化落地 -> 框架映射`

## 目标读者

- 已经做过 Unity 项目，但脑子里还没有“技能系统完整执行链”稳定地图的客户端程序
- 技能逻辑已经写到一定规模，开始被边界、复用、多人、工具和回归问题反复卡住的人
- 想把“会堆功能”升级成“能设计一套长期可维护技能系统”的工程实践者

## 系列边界

### 属于这个系列的内容

- 技能系统到底在解决什么问题
- 技能、效果、Buff、属性、标签、冷却、资源等概念边界
- 定义层、运行时层、上下文层和生命周期骨架
- 输入请求、校验约束、目标选择、命中结算这条执行主链
- Effect 与 Buff 的系统拆分
- 与表现系统、AI、多人同步的交点边界
- 技能编辑器、配置校验、调试视图、回归测试等工程化问题
- Unity 自研技能系统与 Unreal GAS 的思想映射

### 不属于这个系列的内容

- 完整 AI 系列
- 完整网络同步系列
- 完整动画系统系列
- 完整 UI 输入系统系列
- 具体引擎插件、包管理器或商业框架的使用说明书

这组文章只覆盖这些主题和技能系统的交点，不吞掉它们各自的完整体系。

## 上游前置与配套文章

这组文章虽然已经能独立阅读，但更适合挂在下面这些公共底座之上：

- `Command` 模式：理解“施法请求”为什么应该对象化
- `State / FSM / 行为树`：理解生命周期与 AI 调度为什么不能混成一层
- `OCP`：理解为什么技能扩展点不该继续堆回主系统
- `拓扑排序 / 依赖图`：理解技能依赖、配置校验和工具图为什么是同类问题
- `从玩家输入到屏幕画面`：理解输入、运行时和表现链如何接起来

这些前置不属于技能系统系列编号本身，但会决定这组文章的理解速度。

## 系列核心主线

这组文章的主线不按“技能类型”拆，也不按“Unity API”拆。

更稳的主线是：

1. 先回答技能系统在解决什么问题
2. 再切清概念边界
3. 再立数据骨架和生命周期
4. 再把输入、校验、目标、命中接成执行主链
5. 再把世界变化收回 Effect / Buff 两层
6. 再处理表现、AI、网络这些高复杂度交叉边界
7. 最后收束到工具、测试和框架映射

## 当前结构与状态

### 读者入口

| 编号 | 标题 | 角色 | 状态 |
|------|------|------|------|
| 索引 | 技能系统深度系列索引｜先读哪篇，遇到什么问题该回看哪篇 | 阅读地图 | 已写 |

### 已完成主线

| 编号 | 标题 | 角色 | 状态 |
|------|------|------|------|
| 00 | 总论：技能系统到底在解决什么 | 问题空间 | 已写 |
| 01 | 边界：技能、效果、Buff、属性、标签、资源、冷却分别是什么 | 概念边界 | 已写 |
| 02 | 数据模型：SkillDef、SkillInstance、CastContext、TargetSpec、EffectSpec 怎么设计 | 数据骨架 | 已写 |
| 03 | 生命周期：Instant / Cast / Channel / Charge / Toggle / Passive / Combo 怎么统一 | 时序骨架 | 已写 |
| 04 | 输入与施法请求：按键、输入缓冲、技能队列、取消窗口怎么接 | 执行主链 | 已写 |
| 05 | 校验与约束：冷却、消耗、距离、状态、标签阻塞应该放在哪 | 执行主链 | 已写 |
| 06 | 目标选择与命中：单体、范围、投射物、锁定、碰撞、命中时机怎么拆 | 执行主链 | 已写 |
| 07 | 效果系统：伤害、治疗、位移、驱散、召唤为什么应该统一落到 Effect System | 世界变化 | 已写 |
| 08 | Buff / Modifier：叠层、刷新、覆盖、快照、实时重算应该怎么建模 | 世界变化 | 已写 |
| 09 | 动画与表现解耦：前摇点、生效点、受击点、VFX/SFX、Timeline/Event 怎么对齐 | 跨系统边界 | 已写 |
| 10 | AI 与技能系统的边界：AI 负责选技能，不该接管技能执行链 | 跨系统边界 | 已写 |

### 待写主线

| 编号 | 标题 | 角色 | 状态 |
|------|------|------|------|
| 11 | 多人同步：服务器权威、预测、回滚、命中确认、冷却同步应该怎么拆 | 跨系统边界 | 待写 |
| 12 | 编辑器与配置工具：技能编辑器、依赖图、校验器、调试视图怎样服务执行链 | 工程化落地 | 待写 |
| 13 | 测试与回归：公式回归、边界条件、战斗日志、技能时间线怎样落地 | 工程化落地 | 待写 |
| 14 | Unity 自研技能系统 vs Unreal GAS：思想映射，而不是 API 对照 | 映射收束 | 待写 |

### 当前完成度

- 主线已完成 `11 / 15`
- 加上索引页，当前已有 `12` 篇内容
- 当前缺口已经从“技能系统本体是什么”转移到“多人、工具、回归、框架映射”这四块工程后半段

## 推荐后续写作顺序

### 第一优先级｜11 多人同步

先补 `11`，因为这是当前主线最自然的下一个断点。

前面的 `00-10` 已经把单端执行链立住了，接下来最值得补的是：

`当技能系统进入服务器权威和多端同步场景后，这条执行链哪些环节应该上收、哪些环节应该延迟确认、哪些环节绝不能直接复用单机直觉。`

这一篇写完，系列才算从“本地逻辑稳”延伸到“真实项目更常见的对战/联网语境”。

### 第二优先级｜12 编辑器与配置工具

`12` 适合接在 `11` 后面写。

原因不是它依赖网络，而是到这一步为止，数据模型、生命周期、执行链、Buff、表现、AI 边界都已经站住，终于可以回头回答：

`一套技能系统在团队协作里，应该配哪些工具，才能防止配置和依赖关系继续失控。`

这一篇要把工具定位成“服务执行链的工程层”，而不是单独做一篇 Unity 编辑器技巧集合。

### 第三优先级｜13 测试与回归

`13` 应该放在 `12` 后面。

因为一旦多人同步和工具链都开始进入系统，最自然的下一问就是：

`怎样证明这套技能系统没有被下一轮修改悄悄打坏。`

这一篇的重点不是单元测试语法，而是：

- 哪些层最适合做公式回归
- 哪些层最适合做时间线和日志回放
- 哪些层必须在内容侧补校验器

### 第四优先级｜14 Unity 自研技能系统 vs Unreal GAS

`14` 最适合最后写。

它承担的角色不是主线推进，而是对照收束：

`当自研技能系统的核心结构已经写完整之后，再回头把它和 Unreal GAS 做思想映射，才能避免文章退化成 API 对照表。`

所以这篇更像整个系列的横向总结，而不是中途插入的工具文。

## 每篇待写文章的唯一职责

### 11｜多人同步

唯一职责：

`说明技能系统进入服务器权威场景后，施法请求、校验、命中、效果、冷却和表现确认应该怎样重新分层。`

### 12｜编辑器与配置工具

唯一职责：

`说明技能系统为什么必须有编辑器、依赖图、校验器和调试视图，以及这些工具分别服务哪一层执行链。`

### 13｜测试与回归

唯一职责：

`说明技能系统怎样建立一套覆盖公式、时序、边界条件和战斗日志的回归体系。`

### 14｜Unity 自研 vs Unreal GAS

唯一职责：

`把这套自研技能系统主线和 Unreal GAS 做思想映射，回答“哪些概念是同构的，哪些边界是不同的”。`

## 暂不展开的内容

下面这些方向都可能以后写，但不应混进当前主线：

- 具体英雄/职业技能拆解案例
- 完整行为树 / GOAP / Utility AI 体系
- 完整帧同步 / 状态同步 / 回滚网络同步系列
- 单独的动画系统与 Timeline 系列
- 完整战斗数值系统系列

这些内容可以和技能系统交叉，但不应抢走这组文章的主线。

## 当前最短结论

`技能系统深度系列的前半段已经完成，当前最该做的不是再补局部技巧，而是把多人同步、工具链、回归测试和 GAS 映射这四块后半段正式接上。`
