# 本地引擎源码路径

## 用途

这个文件用于告诉自动化任务：

- 本地 `Unity` 源码在哪里
- 本地 `Unreal` 源码在哪里
- 默认引用哪一个版本

如果路径未填写，自动化不应声称自己做过源码级验证。

## Unity

- 状态：`TODO`
- 源码根路径：
- 版本：
- 备注：

## Unreal

- 状态：`TODO`
- 源码根路径：
- 版本：
- 备注：

## 默认规则

- 如果某一项 `状态` 不是 `READY`，自动化应视为该源码不可用
- 如果路径为空，自动化只能使用官方文档和已有证据卡
- 如果路径可用，自动化应优先把关键结论落到源码证据上
